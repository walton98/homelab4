apiVersion: v1
kind: Service
metadata:
  name: jackett-service
  annotations:
    metallb.universe.tf/address-pool: default-pool
spec:
  selector:
    app: jackett
  type: LoadBalancer
  ports:
    - name: docker-port
      protocol: TCP
      port: 9117
      targetPort: 9117
